---
:title: Filtros de conjuntos/arrays
:description:
:keywords: foo, bar
---

# Filtros de conjuntos o arrays (arreglos)

 - paginate
 - size(input)
 - join(input, segmenter = ' ')
 - first(array)
 - last(array)
